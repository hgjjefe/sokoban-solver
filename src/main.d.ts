export {};
declare global {
    interface Window {
        solveFestivalRust: (method: string, gridText: string[], progressCallback: (progress: any) => void, timeoutMs: number) => any;
    }
}
//# sourceMappingURL=main.d.ts.map